# Kanban Spec Framework — Environment Variables
# Copy this file to .env.local and fill in your values.

# ── Vercel Serverless API (required for deployed API) ──
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token
KANBAN_API_TOKEN=your-bearer-token

# ── Spec Server (spec-server.js) ──
KANBAN_API_URL=https://your-kanban.vercel.app
GEMINI_API_KEY=your-gemini-key
OPENROUTER_API_KEY=your-openrouter-key

# ── Task Runner (task-runner.js) ──
# KANBAN_API_URL is shared with spec-server above
# KANBAN_API_TOKEN is shared with the Vercel API above
